name: FPS Open Query Diagnostic

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/fps-open-query-diagnostic.yml"

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Query open records without organism slug
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import requests, json
          url='https://datos.juntadeandalucia.es/api/v0/job-offering-public-sector/search'
          params={
            'announcement_status':'En curso',
            'job_official_degree_requirements':'-',
            'announcement_type':'-',
            'provinces':'-',
            'job_agreement':'-',
            'order_by':'id',
            'mode':'DESC',
            'format':'json',
            'size':200
          }
          r=requests.get(url,params=params,timeout=(10,30))
          print('STATUS=',r.status_code)
          print('URL=',r.url)
          print('BODY=',r.text[:20000])
          if r.ok:
              x=r.json()
              rows=x.get('result',x) if isinstance(x,dict) else x
              if isinstance(rows,list):
                  fps=[q for q in rows if 'progreso y salud' in json.dumps(q,ensure_ascii=False).lower()]
                  print('ROWS=',len(rows))
                  print('FPS_ROWS=',len(fps))
                  print('FPS=',json.dumps(fps[:30],ensure_ascii=False)[:30000])
          PY
